#!/bin/bash

# Displays the name of the latest CI job for the given branch/commit.

function usage() {
    cat<<EOF
    Usage: $0 <commit>
EOF
}

hub ci-status -v $@ | cut -f3 | sed -E -e 's/^.*SweetIQ/SweetIQ/' -e 's/\/[0-9]+\/display\/.*$//'
